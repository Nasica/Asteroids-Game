Class name: Player

Description: The player/ship for asteroids.

Attributes: int score
			int lives
			int MAX_SPEED
			PVector	location
			PVector direction 
			PVector velocity
			boolean accelerating

Methods:	player()			// basic constructor
			player(int) 		// constructor with int lives
			player(int int)     // constructor with int lives and int score
			getScore()
			addScore(int) 
			getLives()
			updateLives(int) 	// int can bepositive or neagative. Could impliment "bonus lives"
			getDirection()
			rotate(int)		    // changes direction by int. left = negative, right = positive.
			getVelocity()
			accelerate(int)     // updates velocity by int, to a maxspeed
			decelerate()        // decelerate to zero when not accelerating
			getBoundingBox()	// returns array of 4 pvectors based on location
			shipDestroyed()     // invokes updateLives. 
			


